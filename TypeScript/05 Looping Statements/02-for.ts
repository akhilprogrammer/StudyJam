

// for loop

    let firstArray = [1, 2, 3, 4, 5];

    for(let i = 0; i < firstArray.length; i++) {

        console.log(firstArray[i]);  

    }



// for - of loop


    // for - of loop iterarte based on length of array
    // on each iteration variable x hold value of each position of array in order from first to last

    let secondArray = [6, 7, 8, 9, 10];

    for(let x of secondArray) {
        console.log(x);
    }



// for  - in loop

    // for - in loop iterarte based on length of array
    // on each iteration variable x hold index of iterartion
    
    let thirdArray = [11, 12, 13, 14, 15];

    for(let x in thirdArray) {
        console.log(thirdArray[x]);
    }
    